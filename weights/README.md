# Model weights

The trained YOLOv26s-OBB checkpoint is distributed through the GitHub Release associated with version 1.0.0.

Download the checkpoint and save it as:

```text
weights/best.pt
```

The checkpoint is not stored directly in the Git repository to avoid adding a large binary file to the Git history.

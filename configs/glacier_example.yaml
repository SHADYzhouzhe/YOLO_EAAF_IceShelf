# Example Ultralytics YOLO-OBB dataset configuration.
# Replace the root path with the location of your local dataset.

path: /path/to/yolo_obb_dataset

train: images/train
val: images/val
test: images/test

names:
  0: damage
